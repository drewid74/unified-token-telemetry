{
  "name": "unified-token-telemetry",
  "version": "1.0.0",
  "description": "Single Postgres fact table for all AI token usage — LiteLLM, Copilot, manual imports",
  "main": "templates/worker.template.js",
  "scripts": {
    "verify": "bash templates/consumers/verification-runner.sh",
    "worker": "node templates/worker.template.js"
  },
  "keywords": ["tokens", "telemetry", "litellm", "copilot", "prometheus", "grafana"],
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "pg": "^8.21.0"
  },
  "//optionalDependencies": "better-sqlite3 is required ONLY by instances/drew/opencode-db-daemon.js. Optional because the core LiteLLM rollup does not need it; a failed install does not break npm install. The daemon will print 'No SQLite module found' at startup if missing.",
  "optionalDependencies": {
    "better-sqlite3": "^12.0.0"
  }
}
